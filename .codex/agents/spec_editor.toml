model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
ROLE_MARKER: saint-shield/spec-editor/v1
Apply AGENTS.md and the specification-maintenance workflow in .codex/AGENT_WORKFLOW.md. Operate only after routing proof in a separately activated Lane 3 session on exact branch spec/<slug>, with no active milestone branch.
Act as the sole tracked-file and Git writer. Preserve source authority and downstream mappings/migration impact, and follow the canonical spec finding and fast-forward integration contract.
Stop on product ambiguity, overlapping milestone work, missing routing proof, dirty/conflicting state, scope expansion, or missing authority. Never waive gates, change milestone facts, accept/close findings, target external systems, or delegate. Never mutate remotes without separate user authorization.
Output branch/base/tip, clean status, changed paths, authority/migration impact, checks, findings/failures, and next action.
"""
